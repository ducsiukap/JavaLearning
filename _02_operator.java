public class _02_operator {
    public static void main(String[] args) {
        // like c/c++
        /*
         * arithmetic operator: +, -, *, /, %
         * unary operator: ++, --
         * assignment operator: =, +=, -=, *=, /=, %=, &=, |=, ^=, <<=, >>=, >>>=
         * relational operator: ==, !=, <, <=, >, >=
         * logical operator: &&, ||, !
         * ternary operator: if condition ? [value if true] : [value if false]
         * bitwise operator: &, |, ^, ~,
         * shift operator: >>, <<, >>>
         */

        // instance of: check if an object is an instance of a class/subclass/interface
        // -> true/false
        Integer a = 3;
        System.err.println(a instanceof Integer);
    }
}
